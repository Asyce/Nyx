# Dependencies
node_modules/

# Cloudflare / Wrangler
.wrangler/
.dev.vars
.dev.vars.*
wrangler.toml.bak

# Secrets (never commit OAuth client secrets, API tokens, etc.)
.env
.env.*
*.secret

# Build output (regenerated by `npm run build`)
Site/dist/
.deploy/

# Logs & OS cruft
*.log
.DS_Store
Thumbs.db

# Editor / tool state
.design-canvas.state.json
.thumbnail/
verification-screenshots/

# GameData beta channel data IS shipped (user-approved 2026-06-22): served behind an
# opt-in Beta/Live toggle that defaults to Live on GameData game pages. Tracked, not ignored.

# Scraper internal state / cache — bookkeeping only, never shipped or needed to regenerate.
Database/_httpcache/
Database/GameData/_state/
Database/GameData/changes/


# Agent coordination (junctions into C:\Pengo\AI) - never commit
.claude/
.agents/
.codex/
.claude.pre-junction-bak/

# Local codebase-intelligence index + project MCP config
.repowise/
.mcp.json

# Temp real-data fixtures for tracker QA (never commit)
Site/_pm.xlsx
Site/_pm_names.json

# design-sync (claude.ai/design import) — staged scripts, build output, machine state
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
