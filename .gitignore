# Dependencies
node_modules/

# Cloudflare / Wrangler
.wrangler/
.dev.vars
.dev.vars.*
wrangler.toml.bak

# Secrets (never commit OAuth client secrets, API tokens, etc.)
.env
.env.*
*.secret

# Build output (regenerated by `npm run build`)
Site/dist/
.deploy/

# Logs & OS cruft
*.log
.DS_Store
Thumbs.db

# Editor / tool state
.design-canvas.state.json
.thumbnail/
verification-screenshots/

# Nanoka beta channel data IS shipped (user-approved 2026-06-22): served behind an
# opt-in Beta/Live toggle that defaults to Live on Nanoka game pages. Tracked, not ignored.

# Scraper internal state / cache — bookkeeping only, never shipped or needed to regenerate.
Database/_httpcache/
Database/Nanoka/_state/
Database/Nanoka/changes/

