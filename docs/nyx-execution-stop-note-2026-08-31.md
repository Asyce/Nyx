# Nyx — saved stopping point

Saved: 2026-09-01, 20:26 CEST / 18:26 UTC.

The goal is blocked, not complete. Public launcher `v1.7` and live Site/receiver `73ce188afcb927ac4363fad6bafb78a771c611ee` are unchanged; nothing was republished or redeployed.

Exact HSR login correction `3144c29e4945cddbbfb985f81a080ccca9959f5a`, local `2748/2748`, hosted run `33539570297`, private development `1.8.0.5`, all `397/397` package files, and the independently reviewed isolation wrapper pass. The fix opens the exact official HSR `Account Log In` form URI directly and adds no DOM click or credential automation.

Prepared operator: `D:\PengoNyx\Nyx Launcher\Validation\phase15-hsr-manual-sync-3144c29\Run-Phase15-HSR-Manual-Sync-Check.ps1`, 12,274 bytes, SHA-256 `46d612c12412307d3e6f5843f4b1d203a3747c1a267538dace926de38231b415`. One normal isolated run is currently active. Computer Use can read the Nyx controls but Windows refuses its focus/activation request. The user must click once on an empty part of the Nyx window and reply `focused`; do not press a launcher button or enter credentials. If ending instead, close Nyx normally and do not reopen it until the wrapper prints `NYX_PHASE15_HSR_CHECK=RESTORED`. The wrapper's active marker and backup are expected while this run is open; normal Nyx data has not been lost.

After focus, verify that HSR Connect opens the official Account Log In form immediately. The user then privately handles login/CAPTCHA, recovery codes, consent, comparison, rotation, and each exact removal confirmation in steps 3-8 of the [execution tracker](nyx-launcher-endfield-hoyolab-execution-tracker-2026-08-24.md#next---hsr-manual-sync-in-one-sitting-exact-private-candidate-active-focus-and-login-proof-pending). Never put a private value or screenshot in chat. Launcher publication remains a separate approval.

STOP 6B, 6D and 10 remain evidence-blocked by missing HSR complete-bag/account wire proof, Genshin current-wire/complete-bag proof, and licensed Endfield achievement catalog/MD5/account proof. Their dependent phases remain closed. The HoYoWiki shadow lane still has two distinct healthy changed observations; the required third has not arrived.

No authentication, upload, deletion, game, installation, stable package, tag, release, product deployment, force-close or shutdown occurred during this resume. The earlier one-time shutdown instruction was consumed on 2026-08-31 and must not be repeated without a new request.

## Previous stopping history

Saved: 2026-08-31, 09:26 CEST / 07:26 UTC.

Historical stop: My HoYo's Phase 15 website support needed deployment approval. The goal was blocked, not finished. The user resumed and supplied `AUTHORIZE: Deploy Phase 15 receiver` later on 2026-08-31. That approval and deployment are now complete; the overall goal is not.

Current stop, 2026-08-31 16:04 CEST / 14:04 UTC: Computer Use reported that the user stopped Windows control with physical Escape. The corrected native check had not started; no new profile isolation or app launch occurred. No further desktop calls were made. The controller stopped safely, original data folders remain in place, all test backup/marker paths are absent, and no Nyx process remains. Resume Windows control in a new turn before the final fake-profile dialog check.

Non-desktop follow-up, 16:13 CEST / 14:13 UTC: the existing hosted launcher run finished successfully; exact source, test counts and artifact metadata are now verified. This automatic goal continuation did not resume Windows control or lift the Escape stop. Original-root/backup/marker/process checks still pass. No new build, rerun, deployment, UI action or shutdown occurred.

Goal blocked, 16:18 CEST / 14:18 UTC: the same user-input boundary has now persisted for three consecutive goal turns: the initial Escape stop, the continuation that finished hosted verification, and this continuation that reconfirmed terminal SUCCESS and clean execution worktrees. No hosted work remains running and no other canonical phase is safely runnable without its missing evidence/approval. The full goal is unfinished. Reply `resume the launcher check` to continue; this is not authorization to publish or to use private account data. The condensed hands-on sequence remains in the tracker below.

The single [production run `33392750660`](https://github.com/Asyce/Nyx/actions/runs/33392750660) passed. Website/receiver `73ce188afcb927ac4363fad6bafb78a771c611ee` is live and verified, including My HoYo, 12 non-mutating API cases and exact feed/image hashes. Do not repeat the deployment or its approval.

- Initial enabled candidate `e7ea64807c7ffafbaf1494f51bff473d051f67db` passed all `2747/2747` local tests, sealed private `1.8.0.0` package verification and hosted run `33397845470`. Its first native check found clipped instructions; it was interrupted by context compaction and exceeded the short fixture window, so no full native pass is claimed. The test launcher closed normally and both original data-root states were restored; backups/marker are absent and no test process remains.
- Reviewed correction `5cfcee737d77d2f0898828a8cdf8d4ae9ba537c7` removes the fixed dialog content width and stretches it to the native viewport. It passes local tests/builds, exact private `1.8.0.1` package verification (`397/397` files), and hosted [run `33399563135`](https://github.com/Asyce/Nyx-Launcher/actions/runs/33399563135), which finished SUCCESS at `14:11:48Z`. Hosted logs confirm `2747/2747` .NET tests, `91` portable Rust tests, the configured audits and development packaging; the two prescribed Npcap integration cases were filtered. Stable packaging and publication skipped. No duplicate run is needed. The local private ZIP SHA-256 remains `36651544c6c128cfb76de7905443a92f01882d10255bbf75167a417c64955e7e`; GitHub's default development `1.4.0.0` artifact is separate source-level CI evidence, not that same private file.
- Native recheck preparation is in the launcher's ignored `Desktop/packaging/artifacts/phase15-native-5cfcee7/RESTORE-NOTE.md`. It has no pending restoration. Its extraction paths exist, so use fresh validated operator paths/run ID instead of blindly rerunning the old controller. The receiver approval does not authorize stable launcher publication or private upload/deletion.
- Do not start/install the enabled candidate against real AppData. The operator uses static package hashes, not updater CLI `verify`, which can reconcile a separate installation. That installation root is absent on this PC; recheck before another package build. Real HSR login, recovery, upload/deletion checks and publication remain separate.
- Public launcher `v1.7` is unchanged. Gear exports and automatic sync remain unavailable. Other evidence gaps and the seven inherited Site test failures remain recorded in the tracker.
- The later hands-on checks are grouped in the [execution tracker](nyx-launcher-endfield-hoyolab-execution-tracker-2026-08-24.md#next---hsr-manual-sync-in-one-sitting-exact-private-candidate-active-focus-and-login-proof-pending).

For this stop only, the user requested one normal shutdown after saving. `shutdown.exe /s /t 0` was issued exactly once, without `/f`, and returned exit 0. The one-shot instruction is consumed and must not be replayed on resume. Unsaved apps may have blocked the shutdown; completion was not observed. Do not force or retry. No permanent or recurring rule was created.
